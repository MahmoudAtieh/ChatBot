# تقرير حالة مشروع Arabic Sofa WhatsApp AI Assistant

**التاريخ:** 16 سبتمبر 2026  
**المرحلة الحالية:** Alpha مكتملة من ناحية الأساس البرمجي، وجاهزة لاختبار تكاملي على بيئة Staging، لكنها ليست جاهزة بعد لربط رقم WhatsApp الحقيقي.

## الملخص التنفيذي

تم بناء النسخة الأساسية من مساعد التسوق وربط مكوّناتها برمجياً: Meta WhatsApp Cloud API، وOpenAI Responses API، وShopify Admin GraphQL بصلاحية قراءة المنتجات، وPostgreSQL. النظام لا يحتاج Fine-tuning أو Vector Database حالياً؛ بل يستخدم أسلوب المتجر والـplaybooks كسياق ثابت، ويستدعي FAQ والسياسات وShopify عند الحاجة فقط. هذه الهيكلية سريعة، بسيطة، وقابلة للتوسع دون Over-engineering.

## ما تم إنجازه

| المجال | الحالة | النتيجة |
|---|---|---|
| معالجة الداتا | مكتمل | تجهيز `style_reference` و`playbook` وحالات التقييم واعتماد FAQ وسياسات الشحن والاسترجاع. |
| أسلوب المحادثة | مكتمل | الردود تستخدم نبرة موظفي Arabic Sofa، مع fast paths للتحية والشكر والتنبيه الأمني. |
| فهم الطلب والـAI | مكتمل برمجياً | Structured Outputs، سياق محادثة قصير، واستدعاء أدوات محدودة بدل إعطاء معلومات غير مؤكدة. |
| Shopify | مكتمل برمجياً | بحث منتجات آمن من السيرفر مع بقاء Shopify مصدر الحقيقة للسعر والمخزون. |
| Meta WhatsApp | مكتمل برمجياً | التحقق من توقيع الـwebhook، فلترة الرسائل، ومنع تكرار `wamid`. |
| Durable Worker | مكتمل | حفظ الرسالة المشفرة قبل `200`، leases، retries بتأخير تدريجي، FIFO لكل عميل، وحالة `dead` بعد استنفاد المحاولات. |
| الخصوصية والأمان | مكتمل كأساس | AES-256-GCM للـqueue، HMAC لمعرف العميل، إزالة بيانات الدفع الظاهرة، وعدم وضع الأسرار في المستودع. |
| التحويل لموظف | جزئي | يتم حفظ السبب والملخص ومراجع الصور، لكن لا توجد بعد وجهة أو لوحة يستلم منها الموظف الطلب. |

مسار الرسالة الحالي هو: **Meta → تحقق وتشفير → PostgreSQL queue → Worker → Fast path أو OpenAI → Shopify/FAQ/Policy/Handoff → Meta → Done/Retry/Dead**. عند فشل إرسال Meta يُعاد استخدام الرد المحفوظ، فلا يتم استدعاء الـAI مرة ثانية. كما أن سجل المحادثة والتحويلات محمي من التكرار باستخدام معرف رسالة Meta.

## جودة النسخة الحالية

- **50 اختباراً ناجحاً** تشمل التشفير، منع التكرار، ترتيب الرسائل، retries، lease recovery، webhook، fast paths وShopify.
- TypeScript typecheck وproduction build ناجحان.
- `npm audit` لا يعرض ثغرات معروفة.
- `/health` و`/ready` تم اختبارهما محلياً بنجاح.
- الكود والداتا مرفوعان إلى مستودع GitHub على فرع `main`.

## ما ينقص قبل الإطلاق الحقيقي

1. تجهيز PostgreSQL 16+ على بيئة Staging، وتشغيل migrations واختبار restart والتزامن بعاملين فعلياً.
2. إضافة مفاتيح Sandbox الخاصة بـMeta وShopify وOpenAI وتنفيذ اختبار كامل من رسالة WhatsApp حتى وصول الرد.
3. ربط جدول `handoffs` بوجهة حقيقية للموظفين مثل لوحة بسيطة أو Slack/Email/نظام خدمة العملاء.
4. ضبط سياسات الاحتفاظ بالمحادثات، النسخ الاحتياطي، المراقبة والتنبيهات قبل الإنتاج.

**ملاحظة تشغيلية:** الإرسال إلى Meta هو `at-least-once`. توجد احتمالية نادرة لتكرار الرد إذا قبلت Meta الرسالة ثم توقف السيرفر قبل تسجيل `done`؛ وهذا لا يمكن إلغاؤه بالكامل دون دعم idempotency من مزود الإرسال.

## الخطوة التالية المقترحة

الانتقال إلى **مرحلة Staging Integration**: إعداد PostgreSQL والمفاتيح التجريبية، تشغيل migrations، ثم إجراء سيناريوهات WhatsApp حقيقية تشمل البحث عن منتج، FAQ، السياسات، فشل Meta وإعادة المحاولة، والـhandoff. بعد نجاحها نربط مسار الموظفين ونجهز قائمة فحص الإطلاق.
